import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({
    formActions: {
        display: 'flex',
        justifyContent: 'space-between',
        padding: '3rem 2rem',
    },
    formContainer: {
        padding: '1rem 2rem',
    },
    formTitle: {
        marginBottom: '2rem',
    }
}))