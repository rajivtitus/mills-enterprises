import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({
    formActions: {
        display: 'flex',
        justifyContent: 'space-between',
        marginTop: '2.5rem',
        padding: '2rem 0rem',
    },
    formContainer: {
        padding: '1rem 2rem',
    },
    formTitle: {
        marginBottom: '3.5rem',
    }
}))