import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({
    footer:{
        minHeight: '5vh',
        position: "static",
        background: "#581818", 
    },
    footerTitle: {
        padding: '1rem',
        color: 'white',
    }
}))