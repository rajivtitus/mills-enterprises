import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({
    loadingCart:{
        minHeight: '90vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    }
}))