import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({
    container: {
        padding: '1rem',
        marginBottom: '7.5rem',
    },
    title: {
        padding: '1.5rem 0rem'
    }
}))